import Grid from './Grid.jsx';
import {createEmptyGrid, BLOCKS_TYPES} from "./logic.js";

const ROWS = 10;
const COLUMNS = 16;
const READY_GRID = 2;


export {
    ROWS,
    COLUMNS,
    READY_GRID,
    Grid,
    createEmptyGrid,
    BLOCKS_TYPES
}